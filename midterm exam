# 1주차

### 셀프 체크 01-1 — 바이브 코딩이란 (교재 p.33)

### 문제 1. 다음 중 AI의 발전 과정을 올바른 순서로 나열한 것은?

① 뉴럴 네트워크 → 딥러닝 → 트랜스포머 → LLM
② 딥러닝 → 뉴럴 네트워크 → LLM → 트랜스포머
③ 트랜스포머 → 뉴럴 네트워크 → 딥러닝 → LLM
④ LLM → 트랜스포머 → 딥러닝 → 뉴럴 네트워크

> **정답: ①**
> 
> 
> **풀이:** **뉴럴 네트워크**(1950s~)가 가장 먼저 등장했고, **딥러닝**(2010s~)으로 이미지·음성 인식 성능이 급상승했으며, **트랜스포머**(2017~)가 문장 전체의 맥락을 한 번에 파악하는 혁신을 가져왔고, 마지막으로 **LLM**(2020~현재)이 사람과 대화하듯 코드를 작성해 주는 단계까지 발전했습니다.
> 

---

### 문제 2. 빈칸을 채워 보세요.

> 바이브 코딩 도구는 여러 AI를 선택할 수 있는 (       ) 도구와
자체 AI를 활용하는 (       ) 도구로 나뉜다.
> 

> **정답: 모델 선택형 / 전용 모델형**
> 
> 
> **풀이:** **모델 선택형** 도구(Cursor, Replit, Windsurf)는 여러 AI 모델 중 원하는 것을 골라 사용할 수 있고, **전용 모델형** 도구(Claude Code, Gemini CLI)는 자체 AI 모델만 사용합니다.
> 

---

### 셀프 체크 01-2 — 나의 첫 웹페이지 (교재 p.54)

### 문제 1. 다음 중 AI 어시스턴트의 발전 과정을 올바른 순서로 나열한 것은?

① 규칙 기반 → 통계 기반 → 딥러닝 기반 → LLM 기반
② 통계 기반 → 규칙 기반 → LLM 기반 → 딥러닝 기반
③ 딥러닝 기반 → LLM 기반 → 규칙 기반 → 통계 기반
④ LLM 기반 → 딥러닝 기반 → 통계 기반 → 규칙 기반

> **정답: ①**
> 
> 
> **풀이:** AI 어시스턴트는 사람이 정한 규칙대로만 동작하는 **규칙 기반**에서 출발해, 데이터를 통계적으로 분석하는 **통계 기반**, 스스로 패턴을 학습하는 **딥러닝 기반**을 거쳐, 자연어 대화가 가능한 **LLM 기반**으로 발전해 왔습니다.
> 

---

### 문제 2. 다음 중 CLI 기반 바이브 코딩 도구의 특징으로 옳지 않은 것은?

① 터미널에서 실행한다
② GUI 없이 텍스트로만 소통한다
③ 여러 AI 모델을 자유롭게 선택할 수 있다
④ 프로젝트 파일을 직접 수정할 수 있다

> **정답: ③**
> 
> 
> **풀이:** CLI 기반 바이브 코딩 도구(Claude Code, Gemini CLI 등)는 **전용 모델형**에 해당합니다. 터미널에서 실행하고(①), GUI 없이 텍스트로 소통하며(②), 프로젝트 파일을 직접 수정할 수 있지만(④), 여러 AI 모델을 자유롭게 선택하는 것은 **모델 선택형 도구**(Cursor, Replit 등)의 특징이므로 ③이 옳지 않은 것입니다.
> 

---

---

---

# 2주차

### 셀프 체크 02-1 — AI를 깨우는 프롬프트의 비밀 (교재 p.66)

### 문제 1. 다음 중 좋은 프롬프트의 조건이 아닌 것을 고르세요.

① 명확성: 원하는 결과를 구체적으로 전달한다.
② 구체성: 필요한 기능이나 조건을 세부적으로 지시한다.
③ 창의성: 결과물이 놀라움을 주도록 요구한다.
④ 맥락 제공: 왜 필요한지, 어떤 상황에서 활용되는지 알려 준다.

> **정답: ③**
> 
> 
> **풀이:** 좋은 프롬프트의 3가지 조건은 **명확성**(①), **구체성**(②), **맥락 제공**(④)입니다. **창의성**은 좋은 프롬프트의 조건에 포함되지 않습니다. "결과물이 놀라움을 주도록 요구한다"는 오히려 모호한 지시에 가깝고, AI가 임의로 판단하게 만들 수 있습니다.
> 

---

### 문제 2. 빈칸을 채워 보세요.

> 프롬프트와 PRD는 모두 (______) 원칙을 기반으로 한다.
이 원칙은 Why, Who, What, When, Where, How 여섯 가지 질문으로 구성된다.
> 

> **정답: 5W1H**
> 
> 
> **풀이:** 5W1H는 Why(왜), Who(누구), What(무엇), When(언제), Where(어디서), How(어떻게)의 여섯 가지 질문으로 구성된 체계적 사고의 틀입니다. 좋은 프롬프트와 PRD(제품 기획서) 모두 이 5W1H 원칙을 기반으로 작성하면 명확하고 구체적인 결과물을 얻을 수 있습니다.
> 

---

### 셀프 체크 02-2 — 마케팅 포트폴리오 (교재 p.80)

### 문제 1. 4단계 전략의 올바른 순서는?

① 디자인 → 기본 틀 → 기능 → 점검
② 기본 틀 → 디자인 → 기능 → 점검
③ 기본 틀 → 기능 → 디자인 → 점검
④ 기능 → 기본 틀 → 점검 → 디자인

> **정답: ③**
> 
> 
> **풀이:** 4단계 포트폴리오 전략의 올바른 순서는 **뼈대(기본 틀) → 기능 → 디자인 → 점검**입니다. 먼저 HTML 기본 구조(뼈대)를 잡고, 실제 데이터를 채워 넣는 기능 단계를 거친 뒤, 색상·레이아웃 등 시각적 요소를 적용하는 디자인 단계, 마지막으로 PRD와 대조하며 오류를 수정하는 점검 단계로 마무리합니다. 이는 컴퓨팅 사고의 '분해'와 '알고리즘'을 실전에 적용한 것입니다.
> 

---

### 문제 2. 효과적인 프롬프트 작성법으로 옳지 않은 것은?

① 5W1H 템플릿을 활용하여 체계적으로 질문한다.
② 모든 기능을 한 번에 요청하여 시간을 절약한다.
③ 각 섹션에 ID를 부여하여 개별 수정이 가능하도록 한다.
④ 구체적인 디자인 레퍼런스를 제공한다.

> **정답: ②**
> 
> 
> **풀이:** 모든 기능을 한 번에 요청하면 HTML, CSS, JavaScript가 뒤섞인 복잡한 코드가 생성되어 확인과 수정이 어려워집니다. 효과적인 프롬프트 작성법은 **4단계 전략(뼈대→기능→디자인→점검)**처럼 단계별로 나누어 요청하는 것입니다. 나머지 보기인 5W1H 템플릿 활용(①), 섹션 ID 부여(③), 구체적 레퍼런스 제공(④)은 모두 효과적인 프롬프트 작성법에 해당합니다.
> 

---

---

---

# 3주차

### 셀프 체크 03-1 — 클로드 코드 설치하기 (교재 p.98~99)

### 문제 1. 클로드 코드를 실행하기 위해 필수적으로 설치해야 하는 두 가지 프로그램을 고르세요.

① Python과 Visual Studio Code
② Node.js와 Git
③ Docker와 Kubernetes
④ Chrome과 Firefox

> **정답: ②**
> 
> 
> **풀이:** 클로드 코드는 npm(Node.js의 패키지 관리자)을 통해 설치하므로 **Node.js**가 필수이고, 프로젝트 버전 관리를 위해 **Git**도 함께 설치해야 합니다. 설치 후 터미널에서 `node --version`, `git --version`으로 정상 설치를 확인할 수 있습니다.
> 

---

### 문제 2. 윈도우 운영체제에서 터미널을 실행하는 명령어는 무엇일까요?

① terminal
② explorer
③ cmd
④ console

> **정답: ③**
> 
> 
> **풀이:** Windows에서 터미널(명령 프롬프트)을 실행하려면 검색창에 **cmd**를 입력하거나, `Windows 키 + R`을 누른 뒤 `cmd`를 입력하면 됩니다. terminal, explorer, console은 Windows 기본 터미널 실행 명령어가 아닙니다.
> 

---

### 문제 3. `npm install -g @anthropic-ai/claude-code`에서 `g` 옵션의 의미는?

① Git과 연동하여 설치
② 그래픽 모드로 설치
③ 시스템 전체에서 사용 가능하도록 설치
④ 게스트 모드로 설치

> **정답: ③**
> 
> 
> **풀이:** `-g`는 **global(전역)**의 약자로, 시스템 전체에서 사용 가능하도록 설치하는 옵션입니다. `-g` 없이 설치하면 해당 프로젝트 폴더에서만 사용할 수 있지만, `-g`를 붙이면 어떤 폴더에서든 `claude` 명령어를 실행할 수 있습니다.
> 

---

### 셀프 체크 03-2 — 손글씨 인식 프로그램 (교재 p.111~112)

### 문제 1. 클로드 코드를 실행할 작업 폴더로 이동하는 방법이 아닌 것을 고르세요.

① 탐색기 주소창에 cmd를 입력하여 해당 폴더에서 터미널 실행
② 터미널을 실행한 뒤 cd 명령어로 원하는 폴더로 이동
③ 클로드 코드가 자동으로 최적 폴더를 선택
④ 원하는 폴더에서 마우스 오른쪽 버튼을 클릭해서 터미널 열기

> **정답: ③**
> 
> 
> **풀이:** 클로드 코드는 **실행된 폴더를 작업 공간으로 인식**하기 때문에, 사용자가 직접 원하는 폴더로 이동한 뒤 실행해야 합니다. 자동으로 최적 폴더를 선택해주는 기능은 없습니다. 탐색기 주소창에 cmd 입력(①), cd 명령어로 이동(②), 마우스 오른쪽 클릭으로 터미널 열기(④)는 모두 유효한 방법입니다.
> 

---

### 문제 2. 손글씨 인식 프로그램 학습 과정에서 관찰할 수 있는 지표로 옳은 설명을 고르세요.

① accuracy는 낮을수록 성능이 좋다.
② loss는 높을수록 성능이 좋다.
③ accuracy는 높을수록, loss는 낮을수록 성능이 좋다.
④ 두 지표 모두 높을수록 성능이 좋다.

> **정답: ③**
> 
> 
> **풀이:** **accuracy(정확도)**는 모델이 얼마나 정확하게 예측했는지를 나타내므로 **높을수록** 좋고, **loss(손실)**는 모델의 예측이 실제 값과 얼마나 차이가 나는지를 나타내므로 **낮을수록** 좋습니다. 학습이 잘 진행되면 accuracy는 올라가고 loss는 내려가는 패턴을 보입니다.
> 

---

### 문제 3. 클로드 코드의 권한 설정 옵션이 아닌 것을 고르세요.

① Ask every time (매번 확인)
② Yes, for this session only (현재 세션만)
③ Always allow in this project (프로젝트 내 항상 허용)
④ Never allow modifications (수정 절대 불가)

> **정답: ④**
> 
> 
> **풀이:** 클로드 코드의 실제 권한 설정 옵션은 **매번 확인**(①), **현재 세션만 허용**(②), **프로젝트 내 항상 허용**(③)의 세 가지입니다. **"수정 절대 불가(Never allow modifications)"**는 존재하지 않는 옵션입니다. 클로드 코드는 파일 접근 시 사용자에게 권한을 묻는 방식으로 보안을 관리합니다.
> 

---

### 셀프 체크 03-3 — CLAUDE.md (교재 p.124~125)

### 문제 1. CLAUDE.md 파일의 주요 역할이 아닌 것을 고르세요.

① 프로젝트 맥락을 자동으로 복원
② 코딩 규칙과 명령어 저장
③ 이전 대화 내용 전체 저장
④ 클로드 코드가 시작할 때 자동으로 읽어들임

> **정답: ③**
> 
> 
> **풀이:** CLAUDE.md는 프로젝트의 구조, 규칙, 명령어를 기록한 설정 파일로, 프로젝트 맥락 복원(①), 코딩 규칙 저장(②), 시작 시 자동 로드(④) 기능을 합니다. 하지만 **이전 대화 내용 전체를 저장하는 기능은 없습니다.** CLAUDE.md는 대화 기록이 아니라 프로젝트의 "매뉴얼" 역할을 하는 문서입니다.
> 

---

### 문제 2. CLAUDE.md 파일을 자동으로 생성하는 명령어를 고르세요.

① /start
② /init
③ /create
④ /setup

> **정답: ②**
> 
> 
> **풀이:** 클로드 코드에서 **/init** 명령어를 입력하면 현재 프로젝트 폴더의 파일과 구조를 자동으로 분석하여 CLAUDE.md 파일을 생성합니다. /start, /create, /setup은 CLAUDE.md 생성과 관련된 명령어가 아닙니다.
> 

---

### 문제 3. `#`의 실제 동작 방식으로 올바른 것을 고르세요.

① 입력한 내용을 즉시 CLAUDE.md에 자동 저장
② 저장 위치를 선택하는 프롬프트를 표시한 후 저장
③ 현재 세션에만 임시로 규칙 적용
④ 모든 프로젝트의 CLAUDE.md를 동시에 업데이트

> **정답: ②**
> 
> 
> **풀이:** `#` 키를 누르면 빠른 규칙 추가 모드가 활성화되고, 규칙을 입력한 뒤 **저장 위치를 선택하는 프롬프트가 표시**됩니다. User memory(모든 프로젝트 공통 적용)와 Project memory(현재 프로젝트에만 적용) 중 선택할 수 있습니다. 즉시 자동 저장(①)이 아니라 위치 선택 과정을 거칩니다.
> 

---

### 문제 4. 계층적 CLAUDE.md 구조의 장점으로 옳지 않은 것을 고르세요.

① 공통 규칙을 루트에 한 번만 작성
② 하위 프로젝트별 특화 규칙 적용 가능
③ 토큰 사용량이 자동으로 감소
④ 전체 프로젝트의 일관성 유지

> **정답: ③**
> 
> 
> **풀이:** 계층적 CLAUDE.md 구조는 루트에 공통 규칙을 두고(①) 하위 폴더마다 특화 규칙을 적용할 수 있으며(②), 전체 프로젝트의 일관성을 유지(④)하는 장점이 있습니다. 하지만 **토큰 사용량이 자동으로 감소하는 것은 계층적 구조의 장점이 아닙니다.** 오히려 여러 CLAUDE.md 파일을 읽어야 하므로 토큰 사용량과는 직접적인 관련이 없습니다.
> 

---

---

---

# 4주차

### 셀프 체크 04-1 — 단계별 프롬프트 (교재 p.145~146)

### 문제 1. 클로드 코드에서 프로젝트 초기 설정을 위해 자주 쓰는 명령어에 대한 설명입니다. 빈칸에 알맞은 명령어들을 채워 보세요.

| 설명 | 명령어 |
| --- | --- |
| 프로젝트 구조를 분석하고 CLAUDE.md 파일을 자동 생성 | /init |
| 현재 세션의 토큰 사용량과 남은 컨텍스트 용량 확인 | /context |
| 사용할 AI 모델을 선택하거나 Default 옵션으로 변경 | /config |
| 사용 가능한 모든 명령어 목록과 설명을 확인 | /help |

> **정답: 위 표 참고**
> 
> 
> **풀이:** `/init`은 프로젝트 폴더를 분석해 CLAUDE.md를 자동 생성합니다. `/context`는 현재 토큰 사용량을 시각적으로 확인할 수 있고, `/config`는 모델 선택 및 환경 설정을 변경합니다. `/help`는 사용 가능한 모든 명령어 목록을 출력합니다.
> 

---

### 문제 2. 다음 설명을 보고 알맞은 용어를 쓰세요.

① 프로젝트 폴더 구조를 분석하여 자동으로 생성되는, 프로젝트의 컨텍스트와 규칙을 담은 마크다운 파일 → (              )

② 제품의 목적, 대상 사용자, 주요 기능, 기술 스펙 등을 체계적으로 정리한 제품 요구사항 문서 → (              )

③ 할 일 관리 앱에서 데이터를 파일로 저장하고 다시 불러올 때 사용하는 데이터 형식 → (              )

> **정답:**
> 
> 
> ① **CLAUDE.md**
> 
> ② **PRD** (Product Requirements Document)
> 
> ③ **JSON**
> 
> **풀이:** CLAUDE.md는 `/init` 명령어로 자동 생성되는 프로젝트 설정 파일입니다. PRD는 제품 기획 단계에서 작성하는 요구사항 문서로, 클로드 웹에서 작성 후 클로드 코드에서 단계별 프롬프트로 변환합니다. JSON은 데이터를 텍스트 형식으로 저장하고 불러오는 데 널리 쓰이는 데이터 형식으로, 할 일 관리 앱의 내보내기/가져오기 기능에 사용됩니다.
> 

---

### 셀프 체크 04-2 — 작업 재개와 효율성 (교재 p.157)

### 문제 1. 다음 설명 중 틀린 것을 고르세요.

① /compact는 대화 내용을 요약하여 토큰을 절약한다.
② auto-compact는 컨텍스트가 임계값에 도달하면 자동으로 실행된다.
③ 압축 후에도 CLAUDE.md와 프로젝트 파일은 유지된다.
④ Ctrl + R 은 /context 명령어의 단축키이다.
⑤ "Accept all" 모드는 Shift + Tab 으로 전환할 수 있다.

> **정답: ④**
> 
> 
> **풀이:** `/compact`는 대화 기록을 요약해 토큰을 절약하고(①), auto-compact는 컨텍스트가 임계값에 도달하면 자동 실행되며(②), 압축 후에도 CLAUDE.md와 프로젝트 파일은 유지됩니다(③). 자동 승인 모드("Accept all")는 **Shift + Tab**으로 전환할 수 있습니다(⑤). 그러나 **Ctrl + R은 /context의 단축키가 아닙니다.** `/context`는 단축키 없이 직접 명령어를 입력해야 합니다.
> 

---

### 문제 2. 다음 상황에서 사용할 적절한 명령어나 옵션을 쓰세요.

| 상황 | 명령어 / 옵션 |
| --- | --- |
| 여러 대화 중 특정 대화를 선택해서 재개하고 싶을 때 | `claude --resume` |
| 가장 최근 대화를 즉시 이어서 작업하고 싶을 때 | `claude --continue` 또는 `claude -c` |
| 현재 컨텍스트 사용량을 시각적으로 확인하고 싶을 때 | `/context` |
| 대화가 길어져서 압축이 필요할 때 | `/compact` |
| 파일 수정 승인을 매번 누르기 번거로울 때 | `Shift + Tab` (자동 승인 모드) |

> **정답: 위 표 참고**
> 
> 
> **풀이:** `--resume`은 이전 대화 목록을 보여주고 선택해서 재개하고, `--continue`(-c)는 가장 최근 대화를 자동으로 복구합니다. `/context`는 현재 토큰 사용량을 컬러 그리드로 시각화하고, `/compact`는 대화를 요약해 컨텍스트 공간을 확보합니다. **Shift + Tab**을 누르면 자동 승인 모드로 전환되어 반복적인 승인 요청을 생략할 수 있습니다.
> 

---

### 셀프 체크 04-3 — 프로젝트 개선과 작업 관리 (교재 p.171)

### 문제 1. 다음 설명 중 틀린 것을 고르세요.

① 스크린 캡처한 이미지는 드래그앤드롭으로 전달할 수 있다.
② @ 기호를 사용하면 파일 검색 시간 없이 즉시 프롬프트에 포함된다.
③ /config는 클로드 코드의 전역 설정만 변경할 수 있다.
④ /doctor는 자동 업데이트 설정 상태를 확인할 수 있다.
⑤ /export로 저장한 파일은 CLAUDE.md 작성 시 참고할 수 있다.

> **정답: ③**
> 
> 
> **풀이:** 캡처 이미지는 드래그앤드롭으로 전달할 수 있고(①), `@` 기호로 파일을 즉시 참조할 수 있으며(②), `/doctor`는 버전·설치 방식·업데이트 상태를 점검하고(④), `/export`로 저장한 대화 기록은 CLAUDE.md 작성 시 참고할 수 있습니다(⑤). 그러나 **`/config`는 전역 설정만이 아니라 프로젝트별 설정도 변경할 수 있으므로** ③이 틀린 설명입니다.
> 

---

### 문제 2. 다음 상황에서 사용할 적절한 명령어나 기능을 쓰세요.

| 상황 | 명령 |
| --- | --- |
| web_version 폴더 구조를 확인하고 싶을 때 | `@web_version` |
| script.js 파일에서 특정 함수를 찾고 싶을 때 | `@script.js` |
| 클로드 코드 설치 상태를 점검하고 싶을 때 | `/doctor` |
| 작업한 대화 내용을 파일로 저장하고 싶을 때 | `/export` |
| 다크 모드로 변경하고 싶을 때 | `/config` |
| 자동 압축 기능을 끄고 싶을 때 | `/config` (Auto-compact 설정 변경) |

> **정답: 위 표 참고**
> 
> 
> **풀이:** `@폴더명` 또는 `@파일명`으로 원하는 파일·폴더를 즉시 참조할 수 있습니다. `/doctor`는 설치 상태와 버전을 진단하고, `/export`는 대화 기록을 클립보드나 파일로 저장합니다. 다크 모드 전환과 자동 압축 기능 on/off는 모두 `/config`(또는 `/settings`) 메뉴에서 변경할 수 있습니다.
> 

---

---

---

# 5주차

### 셀프 체크 05-1 — 할루시네이션과 검증 (교재 p.190)

### 문제 1. 다음 설명 중 틀린 것을 고르세요.

① AI 할루시네이션은 패턴을 과도하게 일반화할 때 발생할 수 있다.
② /memory 명령어에서 Project memory를 선택하면 ./CLAUDE.md 파일이 열린다.
③ /clear는 대화를 요약하여 압축하고 컨텍스트를 유지한다.
④ ./CLAUDE.md에 가이드라인을 추가할 때는 자연어로 간결하고 명확하게 작성하면 된다.

> **정답: ③**
> 
> 
> **풀이:** AI 할루시네이션은 학습 데이터의 패턴을 과도하게 일반화할 때 발생할 수 있고(①), `/memory`에서 Project memory를 선택하면 `./CLAUDE.md` 파일이 열리며(②), CLAUDE.md는 자연어로 간결하게 작성하면 됩니다(④). 그러나 **`/clear`는 대화를 요약·압축하는 것이 아니라 현재 세션의 대화와 컨텍스트를 완전히 초기화**합니다. 대화를 요약하여 압축하는 명령어는 `/compact`입니다.
> 

---

### 문제 2. 다음 상황에서 사용할 적절한 명령어나 대응 방법을 쓰세요.

| 상황 | 대응 |
| --- | --- |
| 퀴즈 게임의 검증 가이드라인을 저장하고 싶을 때 | `/memory` → Project memory 선택 후 CLAUDE.md에 추가 |
| 이전 대화가 새 작업에 영향을 줄 것 같을 때 | `/clear` |
| 대화가 길어져서 토큰을 절약하고 싶을 때 | `/compact` |
| "세계에서 가장 긴 강" 같은 애매한 문제를 발견했을 때 | 측정 기준(전통적 측정 기준 등)을 명시하도록 문제를 수정하는 교차 검증 실시 |

> **정답: 위 표 참고**
> 
> 
> **풀이:** 검증 가이드라인은 `/memory`로 CLAUDE.md에 저장하면 이후 세션에서도 자동으로 적용됩니다. 이전 대화가 방해가 될 때는 `/clear`로 완전 초기화하고, 토큰 절약이 목적이라면 `/compact`로 압축합니다. 애매한 최상급 표현("가장 긴")이 포함된 문제는 "전통적 측정 기준" 등 명확한 조건을 추가해 교차 검증해야 합니다.
> 

---

### 셀프 체크 05-2 — 커스텀 명령어와 자동화 (교재 p.205~206)

### 문제 1. 다음 설명 중 틀린 것을 고르세요.

① 프로젝트 명령어는 해당 프로젝트에서만 사용 가능하다.
② 커스텀 명령어는 자연어로 작성할 수 있다.
③ 명령어 생성 후 즉시 사용 가능하다.
④ 마크다운 파일 형식으로 명령어를 저장한다.
⑤ 매개변수를 통해 유연한 동작이 가능하다.

> **정답: ③**
> 
> 
> **풀이:** 프로젝트 명령어는 해당 프로젝트에서만 사용 가능하고(①), 커스텀 명령어는 자연어로 작성할 수 있으며(②), 마크다운 파일 형식으로 저장하고(④), `$ARGUMENTS`, `$1`, `$2` 같은 플레이스홀더로 유연한 동작이 가능합니다(⑤). 그러나 **커스텀 명령어를 생성한 후에는 `/exit` 후 `claude`로 재시작해야** 새 명령어를 사용할 수 있습니다. 생성 직후 즉시 사용할 수 없습니다.
> 

---

### 문제 2. 다음 빈칸에 들어갈 적절한 답을 쓰세요.

> **정답:**
> 
> - 프로젝트 전용 커스텀 명령어가 저장되어야 할 위치는 **`.claude/commands/`** 이다.
> - 새 명령어 작성 후 사용 가능하게 하려면 클로드 코드를 **재시작(`/exit` 후 `claude`)** 해야 한다.
> - 커스텀 명령어에서 특정 번호 범위의 문제만 검토하려면 **`$1`, `$2`** 와 같이 인수 플레이스홀더를 사용한다.
> - 커스텀 명령어에서 사용자 입력값을 받으려면 **`$ARGUMENTS`** 와 같은 인수 플레이스홀더를 사용한다.
> - `~/.claude/commands`에 만드는 명령어는 **모든 프로젝트** 에서 사용 가능하다.
> - `.claude/commands`에 만드는 명령어는 **해당 프로젝트** 에서 사용 가능하다.
> 
> **풀이:** 프로젝트 전용 명령어는 `.claude/commands/` 폴더에 마크다운 파일로 저장합니다. 새 명령어는 `/exit` 후 재시작해야 인식됩니다. 범위 지정에는 `$1`, `$2`처럼 번호 인수를, 일반 입력값에는 `$ARGUMENTS`를 사용합니다. `~/.claude/commands/`의 전역 명령어는 모든 프로젝트에서, `.claude/commands/`의 프로젝트 명령어는 해당 프로젝트에서만 사용 가능합니다.
> 

---

---

---

# 6주차

### 셀프 체크 06-1 — 클로드 코드에서 API 설정하기 (교재 p.242)

### 문제 1. 오픈라우터에서 API를 사용하기 위해 필요한 것을 모두 고르세요.

① API 키
② 모델명
③ 자체 서버
④ .gitignore 파일

> **정답: ①, ②**
> 
> 
> **풀이:** 오픈라우터에서 API를 사용하려면 사용자를 인증하는 **API 키**와 호출할 **모델명**이 필요합니다. 자체 서버(③)는 필요 없으며(오픈라우터가 서버 역할을 합니다), .gitignore 파일(④)은 API 키 보안을 위한 관리 도구이지 API 사용 자체에 필수적인 것은 아닙니다.
> 

---

### 문제 2. 클로드 코드에서 API 키를 안전하게 관리하기 위한 방법이 아닌 것을 고르세요.

① .env 파일에 API 키 저장
② .gitignore 파일에 .env 추가
③ 코드에 직접 API 키 하드코딩
④ 프로젝트 폴더 내에서만 키 참조

> **정답: ③**
> 
> 
> **풀이:** API 키를 코드에 직접 **하드코딩**하는 것은 보안상 매우 위험한 방법입니다. 코드가 깃허브 등에 업로드되면 API 키가 그대로 노출되기 때문입니다. 안전한 관리 방법은 .env 파일에 저장(①)하고, .gitignore에 등록하여 Git에서 제외(②)하며, 프로젝트 폴더 내에서만 참조(④)하는 것입니다.
> 

---

### 문제 3. 오픈라우터의 Activity 메뉴에서 확인할 수 있는 정보가 아닌 것은?

① 사용한 모델명
② 사용 시간
③ 호출한 코드 내용
④ API 호출 기록

> **정답: ③**
> 
> 
> **풀이:** 오픈라우터의 Activity 메뉴에서는 **사용한 모델명**(①), **사용 시간**(②), **API 호출 기록**(④)을 확인할 수 있습니다. 하지만 **호출한 코드 내용**(③)은 Activity에서 확인할 수 없습니다. Activity는 API 사용 이력을 추적하는 기능이지, 실제 코드 내용을 보여주는 기능이 아닙니다.
> 

---

### 셀프 체크 06-2 — '냉장고를 부탁해' (교재 p.255)

### 문제 1. PRD를 3단계로 분리하여 개발할 때, 2단계에서 텍스트 모델(Qwen 등) API 호출이 실패하면 다음 중 점검이 필요한 항목을 모두 고르세요.

① .env 파일의 OPENROUTER_API_KEY 존재 여부
② 텍스트 모델명의 정확성
③ 1단계에서 Streamlit 서버가 이미 8501 포트를 사용 중인지 여부
④ .gitignore 파일의 존재 여부

> **정답: ①, ②, ③**
> 
> 
> **풀이:** 텍스트 모델 API 호출이 실패하면 **API 키 존재 여부**(①), **모델명의 정확성**(②), **포트 충돌 여부**(③)를 모두 점검해야 합니다. .env 파일에 키가 없거나 모델명에 오타가 있으면 API 호출이 실패하고, 이전 단계의 Streamlit 서버가 포트를 점유 중이면 새 서버 실행에 문제가 생길 수 있습니다. .gitignore 파일(④)은 보안 관리용이지 API 호출 실패와는 직접적인 관련이 없습니다.
> 

---

### 문제 2. Streamlit과 전통적인 웹 개발(HTML/CSS/JS)의 차이점으로 틀린 것을 고르세요.

① Streamlit은 파이썬 코드만으로 웹 앱을 만들 수 있다.
② Streamlit은 데이터 시각화와 ML 모델 시연에 특화되어 있다.
③ Streamlit은 반드시 프런트엔드와 백엔드를 분리해야 한다.
④ Streamlit은 streamlit run 명령어로 실행 가능하다.

> **정답: ③**
> 
> 
> **풀이:** Streamlit의 가장 큰 장점은 프런트엔드와 백엔드를 **분리하지 않고** 파이썬 코드만으로 웹 앱을 만들 수 있다는 것입니다. 따라서 "반드시 프런트엔드와 백엔드를 분리해야 한다"(③)는 틀린 설명입니다. 파이썬만으로 웹 앱 제작(①), 데이터 시각화 및 ML 시연 특화(②), streamlit run 명령어 실행(④)은 모두 올바른 설명입니다.
> 

---

### 문제 3. 프로젝트에서 API 키가 깃허브에 업로드되는 것을 방지하는 구체적인 방법을 서술하세요.

> **정답:** .env 파일에 API 키를 저장하고, .gitignore 파일에 .env를 등록하여 Git 버전 관리에서 제외합니다. 클로드 코드가 API 키 설정 시 이 과정을 자동으로 처리해 줍니다.
> 

---

---

---

# 7주차

### 셀프 체크 07-1 — 서브에이전트 (교재 p.281)

### 문제 1. 커스텀 명령어와 에이전트의 기술적 차이점으로 옳은 것을 고르세요.

① 커스텀 명령어는 .md 파일로 저장되지만 에이전트는 .json 파일로 저장된다.
② 에이전트는 독립적인 AI 모델을 선택할 수 있지만 커스텀 명령어는 메인 세션의 모델을 공유한다.
③ 커스텀 명령어가 에이전트보다 더 복잡한 작업 플로우를 처리할 수 있다.
④ 에이전트는 적용을 위해 재시작이 필요하지만 커스텀 명령어는 바로 사용할 수 있다.

> **정답: ②**
> 
> 
> **풀이:** 서브에이전트는 생성 시 **독립적으로 AI 모델을 선택**(Opus, Sonnet, Inherit 등)할 수 있지만, 커스텀 명령어는 메인 세션에서 사용 중인 모델을 그대로 공유합니다. 커스텀 명령어와 에이전트 모두 .md 파일로 저장되므로 ①은 틀렸고, 에이전트가 더 복잡한 작업을 유연하게 처리할 수 있으므로 ③도 틀렸습니다. 에이전트는 재시작 없이 바로 사용 가능하므로 ④도 틀린 설명입니다.
> 

---

### 문제 2. 에이전트 생성 시 '4. Inherit from parent' 옵션을 선택했을 때의 동작으로 옳은 것을 고르세요.

① 항상 가장 저렴한 모델을 자동 선택한다.
② 메인 세션에서 사용 중인 모델을 그대로 상속받는다.
③ 부모 에이전트가 사용하는 모델을 복사한다.
④ Claude의 최신 모델로 자동 업그레이드된다.

> **정답: ②**
> 
> 
> **풀이:** **Inherit from parent**는 "부모로부터 상속"이라는 뜻으로, 서브에이전트가 **메인 세션에서 사용 중인 모델을 그대로 상속받아** 동일한 모델로 작업합니다. 가장 저렴한 모델을 자동 선택(①)하거나, 최신 모델로 업그레이드(④)하는 기능이 아닙니다. 부모 에이전트가 아닌 메인 세션의 모델을 상속하므로 ③도 정확하지 않습니다.
> 

---

### 문제 3. 에이전트에게 Read-only 권한만 부여했을 때 수행할 수 없는 작업을 모두 고르세요.

① 코드 파일 내용 읽기 및 분석
② 버그 리포트 생성
③ 발견한 버그 수정
④ 새로운 테스트 파일 생성

> **정답: ③, ④**
> 
> 
> **풀이:** Read-only 권한은 **파일을 읽기만** 할 수 있는 권한입니다. 코드 파일 내용을 읽고 분석(①)하거나, 분석 결과를 바탕으로 버그 리포트를 텍스트로 생성(②)하는 것은 가능합니다. 하지만 **발견한 버그를 직접 수정**(③)하거나 **새로운 테스트 파일을 생성**(④)하는 것은 파일 쓰기 권한이 필요하므로 Read-only에서는 수행할 수 없습니다.
> 

---

### 셀프 체크 07-2 — AI 개발팀 (교재 p.300)

### 문제 1. AI 공감 다이어리 개발에서 백엔드 개발과 프런트엔드 개발보다 품질 보증(QA) 테스트 시간이 더 오래 걸린 이유로 가장 적절한 것을 고르세요.

① QA 에이전트가 더 느린 AI 모델을 사용하기 때문이다.
② QA 에이전트는 이전 에이전트들의 작업 결과를 모두 검토해야 하기 때문이다.
③ QA 에이전트만 인터넷 연결이 필요하기 때문이다.
④ QA 에이전트는 파일 읽기 권한이 없기 때문이다.

> **정답: ②**
> 
> 
> **풀이:** QA(품질 보증) 에이전트는 백엔드와 프런트엔드 에이전트가 구현한 **모든 작업 결과를 종합적으로 검토**해야 하기 때문에 더 오래 걸립니다. 5개 에이전트 모두 동일한 Sonnet 모델을 사용하므로 ①은 틀렸고, QA만 인터넷이 필요한 것은 아니므로 ③도 틀렸습니다. QA 에이전트는 All tools 권한으로 생성하므로 파일 읽기가 가능하여 ④도 틀린 설명입니다.
> 

---

### 문제 2. PDF 요약 도구에서 제품 기획 관리자 에이전트가 생성한 PRD를 다른 에이전트들이 참조하는 방식으로 옳지 않은 것을 고르세요.

① PRD가 프로젝트 폴더에 생성되어 각 에이전트들이 접근한다.
② 에이전트들이 순차적, 혹은 병렬로 작업하며 PRD를 참조하게 된다.
③ PRD 파일 경로를 에이전트별로 지정하지 않아도 된다.
④ PRD는 '.claude/agents/' 폴더에 자동으로 복사되어 공유된다.

> **정답: ④**
> 
> 
> **풀이:** PRD는 **프로젝트 폴더에 생성**되어 모든 에이전트가 해당 파일에 접근할 수 있습니다(①). 에이전트들은 순차적 또는 병렬로 작업하며 PRD를 참조하고(②), 파일 경로를 별도로 지정하지 않아도 프로젝트 폴더 내에서 자동으로 찾을 수 있습니다(③). 하지만 **PRD가 '.claude/agents/' 폴더에 자동으로 복사되는 기능은 없습니다**(④). '.claude/agents/' 폴더는 에이전트 설정 파일(.md)이 저장되는 곳이지, PRD 문서가 복사되는 곳이 아닙니다.
> 

---

### 셀프 체크 08-1 — MCP 개념 (교재 p.321)

### 문제 1. MCP와 API의 차이점으로 가장 적절한 설명을 고르세요.

① API는 특정 기능을 요청하는 방식이고, MCP는 AI가 외부 시스템과 직접 소통하게 하는 프로토콜
② MCP는 API보다 속도가 빠르다
③ API는 유료이고 MCP는 무료이다
④ MCP는 클로드 코드에서만 사용 가능하다

> **정답: ①**
> 
> 
> **풀이:** API는 **특정 서비스 하나와 연결하는 대화 창구**이고, MCP는 **여러 외부 데이터·도구·서비스를 AI가 직접 선택하여 활용할 수 있게 하는 표준화된 통신 규약(프로토콜)**입니다. MCP가 반드시 API보다 빠른 것은 아니며(②), API와 MCP 모두 유무료가 존재하고(③), MCP는 클로드 코드 외에도 다양한 AI 도구에서 사용 가능하므로(④) 모두 틀린 설명입니다.
> 

---

### 문제 2. 로컬 MCP 서버 설치 명령어에서 `-` 구분자의 역할로 옳은 것을 고르세요.

① 주석을 표시하는 기호
② 클로드 코드 옵션과 MCP 서버 실행 명령을 구분
③ 버전 정보를 나타내는 표시
④ 에러 처리를 위한 구분자

> **정답: ②**
> 
> 
> **풀이:** `--` 구분자는 **클로드 코드의 옵션**(-s local 등)과 **MCP 서버를 실행하는 명령**(`cmd /c npx -y ...` 등)을 **구분하는 역할**을 합니다. 주석 표시(①), 버전 정보(③), 에러 처리(④)와는 관련이 없습니다. `--` 앞쪽은 클로드 코드가 해석하고, 뒤쪽은 MCP 서버 실행 명령으로 전달됩니다.
> 

---

### 문제 3. Sequential Thinking MCP를 사용했을 때와 일반 추론의 차이점을 서술하세요.

> **정답:** Sequential Thinking MCP는 복잡한 문제를 **여러 단계로 나누어 순차적으로 사고**하므로, 일반 추론보다 더 체계적이고 상세한 결과를 생성합니다. 교재의 비교 실험에서 일반 추론은 총 85줄, 순차적 추론은 총 222줄(약 2.6배)로 훨씬 깊이 있는 분석 결과를 보여주었습니다.
> 

---

### 셀프 체크 08-2 — 테스트와 버전 관리 (교재 p.345)

### 문제 1. Playwright MCP가 테스트 완료 후 생성하는 폴더 이름을 고르세요.

① .test-results
② .playwright-mcp
③ .automation
④ .screenshots

> **정답: ②**
> 
> 
> **풀이:** Playwright MCP는 자동 테스트를 수행한 후 스크린샷 등 결과물을 **`.playwright-mcp`** 폴더에 저장합니다. .test-results(①), .automation(③), .screenshots(④)는 Playwright MCP가 생성하는 폴더 이름이 아닙니다.
> 

---

### 문제 2. 깃허브에 코드를 업로드하는 순서로 올바른 것을 고르세요.

① commit → add → push
② push → add → commit
③ add → commit → push
④ add → push → commit

> **정답: ③**
> 
> 
> **풀이:** 깃허브에 코드를 업로드하는 올바른 순서는 **add(추가) → commit(커밋) → push(푸시)**입니다. 먼저 `git add`로 업로드할 파일을 준비 공간(staging area)에 추가하고, `git commit`으로 현재 상태를 로컬 저장소에 저장한 뒤, `git push`로 로컬 저장소의 내용을 원격 저장소(깃허브)에 업로드합니다.
> 

---

### 문제 3. MCP 서버 주소를 찾는 가장 효과적인 방법 두 가지를 설명하세요.

> **정답:** 첫째, **구글 검색**에서 "[서비스명] MCP"로 검색하여 관련 깃허브 문서를 찾는 방법입니다. 둘째, **깃허브 저장소**에서 해당 MCP 서버의 README 문서를 확인하여 설치 명령어와 서버 주소를 확인하는 방법입니다. 이 두 가지가 가장 효과적인 MCP 서버 탐색 방법입니다.
> 

---

### 셀프 체크 08-3 — 배포와 데이터베이스 (교재 p.368)

### 문제 1. 로컬 스토리지와 클라우드 데이터베이스의 가장 큰 차이점은?

① 로컬 스토리지가 더 빠르다.
② 데이터베이스는 여러 기기에서 데이터를 공유할 수 있다.
③ 로컬 스토리지가 더 안전하다.
④ 데이터베이스는 무료가 없다.

> **정답: ②**
> 
> 
> **풀이:** 로컬 스토리지는 **브라우저 내부에만 데이터가 저장**되어 같은 브라우저에서만 접근 가능하고, 캐시 삭제 시 데이터가 사라집니다. 반면 클라우드 데이터베이스는 **인터넷 서버에 데이터가 저장**되어 **여러 기기에서 동일한 데이터를 공유**할 수 있고 영구 보관됩니다. 로컬 스토리지가 반드시 더 빠르거나(①) 안전한 것은 아니며(③), Supabase처럼 무료 플랜을 제공하는 클라우드 데이터베이스도 있으므로(④) 틀린 설명입니다.
> 

---

### 문제 2. Vercel 배포 시 404 에러가 발생한 이유와 해결 방법으로 옳은 것을 고르세요.

① 서버 오류 – 재시작 필요
② index.html 파일 부재 – 파일명 변경 필요
③ 인증 실패 – 재로그인 필요
④ 네트워크 오류 – 연결 확인 필요

> **정답: ②**
> 
> 
> **풀이:** Vercel 배포 시 404 에러는 주로 **index.html 파일이 없거나 파일명이 올바르지 않을 때** 발생합니다. Vercel은 기본적으로 루트 디렉터리에서 index.html 파일을 찾아 표시하므로, 해당 파일이 없으면 404 에러가 나타납니다. 서버 오류(①), 인증 실패(③), 네트워크 오류(④)는 404 에러의 주된 원인이 아닙니다.
> 

---

### 문제 3. Supabase의 테이블과 엑셀 시트의 유사점과 차이점을 설명하세요.

> **정답:** **유사점**은 둘 다 데이터를 **행과 열로 구성된 표 형태**로 저장한다는 점입니다. 엑셀의 시트 하나가 Supabase의 테이블 하나와 유사한 구조입니다. **차이점**은 엑셀 시트는 로컬 파일에 저장되어 해당 컴퓨터에서만 접근 가능하지만, Supabase 테이블은 **클라우드 서버에 저장**되어 어디서든 API를 통해 데이터를 저장·조회할 수 있고, 여러 사용자가 동시에 접근할 수 있다는 점입니다.
>
